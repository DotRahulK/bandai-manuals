# Discord bot — minimal env for Supabase-backed deployment

# --- Discord ---
DISCORD_APP_ID=
DISCORD_TOKEN=
# Optional: register/test slash commands faster in a single guild
# DISCORD_GUILD_ID=

# --- Data source (Supabase) ---
# Use the service role key (server-side only)
SUPABASE_URL=
SUPABASE_KEY=

# Force the bot to use Supabase-js (ignore pg even if DATABASE_URL exists)
USE_SUPABASE_JS=1

# --- PDF uploads ---
# Cap attachment size in MB (Discord often limits to 8MB)
ATTACH_MAX_MB=8
# Try to attach a local file if present under FILES_ROOT/SUBDIR
ATTACH_IF_LOCAL=0

# If you keep some PDFs on disk (optional)
FILES_ROOT=/srv/manuals
SUBDIR=manuals

# --- Optional pg fallback (not needed if using Supabase)
# DATABASE_URL=

