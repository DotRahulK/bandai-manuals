# Copy to .env and fill as needed

# --- Discord Bot ---
# Required for command registration and bot runtime
DISCORD_APP_ID=
DISCORD_TOKEN=

# Optional (for HTTP Interactions or future use)
DISCORD_PUBLIC_KEY=
DISCORD_GUILD_ID=
DISCORD_PERMISSIONS=

# Attach local PDFs automatically (0/1) and size cap in MB
ATTACH_IF_LOCAL=0
ATTACH_MAX_MB=8
ALWAYS_UPLOAD=0
DOWNLOAD_ON_DEMAND=0

# --- Database (Postgres) ---
# Either provide DATABASE_URL or the PG* fields
DATABASE_URL=
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=postgres
PGPOOL_MAX=10

# --- Supabase target (for sync) ---
# Provide either SUPABASE_DATABASE_URL or SUPABASE_PG* to connect target DB
SUPABASE_DATABASE_URL=
SUPABASE_PGHOST=
SUPABASE_PGPORT=5432
SUPABASE_PGUSER=postgres
SUPABASE_PGPASSWORD=
SUPABASE_PGDATABASE=postgres
SUPABASE_PGSSL=1

# Optional: separate source DB for sync (defaults to DATABASE_URL/PG* above)
SOURCE_DATABASE_URL=
SOURCE_PGHOST=
SOURCE_PGPORT=
SOURCE_PGUSER=
SOURCE_PGPASSWORD=
SOURCE_PGDATABASE=

# --- Files storage ---
# Files are stored relative to FILES_ROOT/SUBDIR and the DB persists relative paths
FILES_ROOT=downloads
SUBDIR=manuals

# --- Supabase Storage (optional) ---
# For uploading local PDFs to Supabase Storage and using public URLs
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_BUCKET=manuals
# Optional object key prefix within the bucket, e.g., "bandai"
SUPABASE_PREFIX=
UPLOAD_CONCURRENCY=2
OVERWRITE=0
DRY_RUN=0

# --- Scraper/Populate ---
# Listing URL override (default is baked in scraper)
# BASE_LIST_URL=https://manual.bandai-hobby.net/?sort=new&...
CONCURRENCY=4
DL_CONCURRENCY=3
DELAY_MS=250
TIMEOUT_MS=30000
USER_AGENT=
DOWNLOAD=0

# --- Downloader from DB ---
ONLY_MISSING=1
# LIMIT=50
# GRADE=HG,MG
# IDS=3962,4010

# --- Generic Crawler (src/index.ts) ---
BASE_URL=https://manual.bandai-hobby.net/
